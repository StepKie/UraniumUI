<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:UraniumApp"
             xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             x:Class="UraniumApp.Pages.DropdownFieldPage"
             Title="DropdownFieldPage">

    <ContentPage.Resources>
        <x:String x:Key="DocLink">https://enisn-projects.io/docs/en/uranium/2.9/infrastructure/Dropdown</x:String>
        <x:String x:Key="SourceLink">https://github.com/enisn/UraniumUI/blob/develop/demo/UraniumApp/Pages/DropdownFieldPage.xaml</x:String>
        <DataTemplate x:Key="RichDropdownItemTemplate">
            <Grid Padding="12" ColumnDefinitions="36,*" ColumnSpacing="12">
                <Border WidthRequest="32" HeightRequest="32" StrokeThickness="0" BackgroundColor="{Binding Color}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16" />
                    </Border.StrokeShape>
                    <Label Text="{Binding Initial}" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="{Binding Name}" FontAttributes="Bold" />
                    <Label Text="{Binding Description}" FontSize="Micro" Opacity=".65" />
                </VerticalStackLayout>
            </Grid>
        </DataTemplate>
    </ContentPage.Resources>

    <Grid RowDefinitions="*">
        <material:TabView TabPlacement="Top">
            <material:TabItem Title="DropdownField">

                <ScrollView>
                    <VerticalStackLayout Padding="20">
                        <Label Text="DropdownField" FontSize="Title" />
                        <Label Text="UraniumUI.Material.Controls" FontSize="Micro" Opacity=".6" />

                        <HorizontalStackLayout Padding="0,20">
                            <Button Text="Documentation" Command="{x:Static uranium:Commands.OpenLinkCommand}" CommandParameter="{StaticResource DocLink}" StyleClass="OutlinedButton" ImageSource="{FontImageSource Glyph={x:Static uranium:MaterialSharp.File_open}, FontFamily=MaterialSharp, Color={AppThemeBinding {StaticResource OnBackground}, Dark={StaticResource OnBackgroundDark}}}" />
                            <Button Text="Source" StyleClass="OutlinedButton" Command="{x:Static uranium:Commands.OpenLinkCommand}" CommandParameter="{StaticResource SourceLink}" ImageSource="{FontImageSource Glyph={x:Static uranium:MaterialSharp.Code}, FontFamily=MaterialSharp, Color={AppThemeBinding {StaticResource OnBackground}, Dark={StaticResource OnBackgroundDark}}}" />
                        </HorizontalStackLayout>

                        <StackLayout HorizontalOptions="Start">

                            <material:DropdownField Title="Choose" AllowClear="True" StyleClass="ControlPreview">
                                <material:DropdownField.ItemsSource>
                                    <x:Array Type="{x:Type x:String}">
                                        <x:String>Option 1</x:String>
                                        <x:String>Option 2</x:String>
                                        <x:String>Option 3</x:String>
                                    </x:Array>
                                </material:DropdownField.ItemsSource>
                            </material:DropdownField>
                        </StackLayout>

                        <BoxView StyleClass="Divider"/>

                        <!--<local:ViewShowcaseView>

                            <material:DropdownField x:Name="demoDropdownField" />

                            <local:ViewShowcaseView.SidePanel>
                                <VerticalStackLayout>
                                    <local:PropertyEditorView 
                                        MaximumHeightRequest="600"
                                        HierarchyLimitType="{Type material:InputField}"
                                        Hierarchical="True"
                                        Value="{x:Reference demoDropdownField}"
                                        />
                                </VerticalStackLayout>
                            </local:ViewShowcaseView.SidePanel>

                        </local:ViewShowcaseView>-->


                    </VerticalStackLayout>
                </ScrollView>

            </material:TabItem>
            <material:TabItem Title="MauiDropdown">
                <ScrollView>
                    <VerticalStackLayout Padding="20" Spacing="20">
                        <Label Text="MauiDropdown" FontSize="Title" />
                        <Label Text="Pure MAUI dropdown with overlay-hosted templated items" FontSize="Micro" Opacity=".6" />

                        <Label Text="Scroll this page, then open the dropdown. The overlay should follow the field's visible screen position." />

                        <BoxView HeightRequest="220" Color="Transparent" />

                        <uranium:MauiDropdown
                            WidthRequest="320"
                            Placeholder="Choose a profile"
                            ItemsSource="{Binding TemplatedItems}"
                            SelectedItem="{Binding SelectedTemplatedItem}"
                            ItemTemplate="{StaticResource RichDropdownItemTemplate}"
                            DropDownBackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource SurfaceDark}}"
                            DropDownBorderColor="{AppThemeBinding Light={StaticResource Outline}, Dark={StaticResource OutlineDark}}"
                            SelectedItemBackgroundColor="{AppThemeBinding Light={StaticResource PrimaryContainer}, Dark={StaticResource PrimaryContainerDark}}"
                            HoveredItemBackgroundColor="{AppThemeBinding Light={StaticResource SurfaceVariant}, Dark={StaticResource SurfaceVariantDark}}"
                            PressedItemBackgroundColor="{AppThemeBinding Light={StaticResource PrimaryContainer}, Dark={StaticResource PrimaryContainerDark}}" />

                        <material:MauiDropdownField
                            Title="Material field"
                            WidthRequest="320"
                            AllowClear="True"
                            ItemsSource="{Binding TemplatedItems}"
                            SelectedItem="{Binding SelectedTemplatedItem}"
                            ItemTemplate="{StaticResource RichDropdownItemTemplate}" />

                        <BoxView HeightRequest="500" Color="Transparent" />
                    </VerticalStackLayout>
                </ScrollView>
            </material:TabItem>
            <material:TabItem Title="Dropdown">
                <VerticalStackLayout Padding="20">
                    <Label Text="Dropdown" FontSize="Title" />
                    <Label Text="UraniumUI.Controls" FontSize="Micro" Opacity=".6" />

                    <VerticalStackLayout StyleClass="ControlPreview" HorizontalOptions="Center" Spacing="32">

                        <uranium:Dropdown Placeholder="Choose" x:Name="dropdown">
                            <uranium:Dropdown.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>Item 1</x:String>
                                    <x:String>Item 2</x:String>
                                    <x:String>Item 3</x:String>
                                </x:Array>
                            </uranium:Dropdown.ItemsSource>
                        </uranium:Dropdown>

                        <Button StyleClass="TextButton" Text="Clear" Clicked="Button_Clicked" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </material:TabItem>
        </material:TabView>

    </Grid>
</ContentPage>
